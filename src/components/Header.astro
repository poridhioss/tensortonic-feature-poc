---

---

<header class="header">
  <div class="logo">
    <a href="/">
      <span class="logo-icon">∑</span>
      <span class="logo-text">ML Math</span>
    </a>
  </div>
  <nav class="nav">
    <a href="/ml-math" class="nav-link">
      ML Math
      <span class="badge">New</span>
    </a>
    <a href="/demos/visualizations" class="nav-link">Demos</a>
    <a href="#" class="nav-link">About</a>
  </nav>
</header>

<style>
  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 2rem;
    height: 60px;
    background-color: var(--zinc-900);
    border-bottom: 1px solid var(--zinc-800);
    position: sticky;
    top: 0;
    z-index: 100;
  }

  .logo a {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: var(--zinc-50);
    font-weight: 600;
    font-size: 1.25rem;
  }

  .logo-icon {
    font-size: 1.5rem;
    color: var(--emerald-500);
  }

  .nav {
    display: flex;
    gap: 1.5rem;
  }

  .nav-link {
    color: var(--zinc-400);
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: color 0.2s;
  }

  .nav-link:hover {
    color: var(--zinc-100);
    text-decoration: none;
  }

  .badge {
    background: linear-gradient(135deg, var(--emerald-500), var(--cyan-500));
    color: var(--zinc-950);
    font-size: 0.65rem;
    font-weight: 700;
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    text-transform: uppercase;
  }

  @media (max-width: 768px) {
    .header {
      padding: 0 1rem;
    }

    .nav {
      gap: 1rem;
    }
  }
</style>
